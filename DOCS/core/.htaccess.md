# File Report: core/.htaccess

## Purpose
Security configuration for the `core/` directory.

## Overview
Denies all direct HTTP access to the `core/` directory to prevent exposure of framework source code.

## File Location
`core/.htaccess`

## Rules
- `Require all denied`

## Source References
- `core/.htaccess:1-4`
