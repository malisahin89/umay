# Defense in Depth: Deny all direct HTTP access to this directory
# Derinlemesine Savunma: Bu dizine tüm doğrudan HTTP erişimini engelle
# Even if the main .htaccess redirection fails (e.g., misconfigured Nginx),
# this file prevents direct access to log files, cache, and other sensitive data.
Require all denied
